# Copy this file to .env and fill in your values.
# Never commit the actual .env file — it's in .gitignore.

MONGODB_URI=mongodb+srv://<username>:<password>@cluster0.xxxxx.mongodb.net/profsync?retryWrites=true&w=majority
PORT=5000
